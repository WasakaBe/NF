import { Rect } from "react";
import { TextInput } from "react-native/types";

const Input = (props)=>
<TextInput
    placeholder={props.texto}
    style={style.input}
    onc

>

</TextInput>