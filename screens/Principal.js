import React from "react";
import { ScrollView, Text } from "react-native";
const Principal =()=>{
    return(
   <ScrollView>
    <Text>hola</Text>
   </ScrollView>
    );
}
export default Principal;